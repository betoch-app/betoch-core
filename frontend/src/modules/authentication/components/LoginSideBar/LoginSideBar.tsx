import Logo from '../../../core/views/components/Logo/Logo';

const LoginSideBar = () => {
  return (
    <div className="w-full h-full p-8 login-bg overflow-auto">
      <Logo secondWordColor="#B9004E" firstWordColor="#fff" />
    </div>
  );
};

export default LoginSideBar;
